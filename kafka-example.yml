---
- name: Read messages from a kafka topic and act on them
  hosts: kafka.example.com
  ## Define our source for events
  sources:
    - ansible.eda.kafka:
        host: kafka.lab.cheeseburgia.com
        port: 9092
        topic: test
        group_id:

  rules:
    - name: receive event
      condition: event.message != ""
      action:
        run_job_template:
          name: {insert here}
...
